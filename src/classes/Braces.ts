import { defaultBraces } from '../const';

export class Braces {
    public timestamp: string[] = defaultBraces;
    public context: string[] = defaultBraces;
    public level: string[] = defaultBraces;
}